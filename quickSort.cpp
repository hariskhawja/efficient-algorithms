#include <iostream>
#include <string>
#include <vector>

void quickSort(std::vector<int> &vect) {

    for(int i = 0; i < vect.size(); i++) {

    }

    vect.push_back(69);
    
}

int main() {

    std::vector<int> vect = {1, 2, 3, 4, 5, 6, 7, 8, 9};

    quickSort(vect);

    return 0;
}